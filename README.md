# 🔥 FireGuard 消防员出警安全计时系统

> 一款部署于 Android 设备上的本地化安全监控工具，通过 NFC 刷卡触发倒计时机制，实现对出警消防员的实时安全监护，超时未归自动报警，保障一线人员生命安全。

## ✨ 核心特性

- **🚫 零网络依赖**：断网、无信号环境下仍可运行
- **⚡ 秒级响应**：刷卡即启动，超时即报警
- **🎯 极简操作**：无需培训，值班员"贴卡即用"
- **🔋 高可靠性**：7×24 小时前台服务保活，不漏报
- **📱 多并发监控**：同时监控最多 20 名消防员
- **🔔 多重报警**：自动拨号 + 全屏弹窗 + 警报音 + 震动

## 📋 功能清单

### 核心功能
- ✅ NFC 读卡（出警触发）
- ✅ NFC 写卡（注册新卡）
- ✅ 倒计时监控（实时显示剩余时间）
- ✅ 超时报警（自动拨号、全屏弹窗、警报音、震动）
- ✅ 历史记录（出警记录、报警记录）
- ✅ 设置管理（倒计时时长、紧急电话配置）

### 技术特性
- ✅ 前台服务保活机制
- ✅ SQLite 本地数据库存储
- ✅ 暗色主题设计
- ✅ 大字体、高对比度 UI
- ✅ 支持 Android 8.0+

## 🛠️ 技术栈

- **框架**: Flutter 3.10.4+
- **状态管理**: Provider
- **数据库**: SQLite (sqflite)
- **NFC**: nfc_manager
- **前台服务**: flutter_foreground_task
- **本地存储**: SharedPreferences

## 📱 系统要求

- Android 8.0+ 设备
- 支持 NFC 写入的设备（如华为 Mate 系列、小米数字系列）
- NTAG213/215/216 等 NDEF 卡
- 建议使用平板设备（7-10英寸）

## 🚀 快速开始

### 安装依赖

```bash
flutter pub get
```

### 运行应用

```bash
flutter run
```

### 构建 APK

```bash
flutter build apk --release
```

## 📖 使用说明

### 日常使用流程

1. **启动应用**：应用启动后自动启动前台服务，开始监听 NFC
2. **出警刷卡**：消防员出警时，将 NFC 卡贴近设备背部
3. **监控计时**：屏幕显示"张三 剩余：20:00"即表示已开始监控
4. **返回刷卡**：返回时再次刷卡，计时重置
5. **超时报警**：若超时未返回，自动触发报警（拨号、弹窗、警报音）

### 注册新卡流程

1. 点击"注册新消防员"按钮
2. 输入消防员姓名
3. 将空白 NFC 卡贴近设备背部
4. 点击"检测卡片"确认卡片为空
5. 点击"写入卡片"完成注册

## 📁 项目结构

```
lib/
├── main.dart                 # 应用入口
├── constants/               # 常量定义
│   ├── app_constants.dart  # 应用常量
│   ├── app_colors.dart     # 颜色定义
│   └── app_theme.dart      # 主题配置
├── models/                 # 数据模型
│   ├── firefighter.dart    # 消防员模型
│   ├── timer_record.dart   # 计时记录模型
│   ├── alarm_record.dart   # 报警记录模型
│   └── history_record.dart # 历史记录模型
├── services/              # 核心服务层
│   ├── database_service.dart    # 数据库服务
│   ├── nfc_service.dart         # NFC 读写服务
│   ├── timer_service.dart        # 计时器服务
│   ├── alarm_service.dart       # 报警服务
│   ├── settings_service.dart    # 设置服务
│   └── foreground_service.dart  # 前台服务
├── providers/             # 状态管理
│   ├── timer_provider.dart  # 计时器状态
│   └── alarm_provider.dart  # 报警状态
├── screens/              # 页面
│   ├── home_screen.dart      # 主监控屏
│   ├── register_screen.dart  # 注册页面
│   ├── settings_screen.dart  # 设置页面
│   └── history_screen.dart   # 历史记录页面
├── widgets/              # 可复用组件
│   ├── timer_card.dart       # 计时器卡片
│   └── alarm_dialog.dart    # 报警弹窗
└── utils/                # 工具类
    └── uid_generator.dart    # UID 生成器
```

## 📄 文档

- [产品需求文档 (PRD)](pd.markdown)
- [架构设计文档](ARCHITECTURE.md)

## 🔐 权限说明

应用需要以下权限：
- **NFC 权限**：读取和写入 NFC 标签
- **电话权限**：超时自动拨打紧急电话
- **通知权限**：显示前台服务通知
- **震动权限**：超时报警震动
- **Wake Lock**：防止系统休眠

## ⚠️ 注意事项

1. **NFC 测试**：需要真实 Android 设备和 NFC 标签进行测试
2. **权限处理**：首次运行时需要用户授权相关权限
3. **后台保活**：应用使用前台服务保持运行，会在通知栏显示持续通知
4. **数据存储**：所有数据存储在本地，无网络依赖

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📝 许可证

本项目为私有项目，仅供内部使用。

## 👥 作者

- 开发团队

## 📞 联系方式

如有问题或建议，请提交 Issue。

---

**⚠️ 重要提示**：本系统为**生命安全关键系统**，所有设计以**可靠性 > 功能性 > 美观性**为优先级。
